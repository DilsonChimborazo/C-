#ifndef SUDOKU_HPP
#define SUDOKU_HPP

#include <array>
using namespace std;

// Declaración de la función que verifica si un tablero de Sudoku es válido
bool esTableroValido(const array<array<int, 9>, 9>& tablero);

#endif
